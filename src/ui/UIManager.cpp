#include "UIManager.h"

UIManager::UIManager() {
    // Initialize font and text objects
}

UIManager::~UIManager() {
    // Clean up resources
}

void UIManager::render(sf::RenderWindow& window, int score, int highScore, int lives) {
    
}